<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.m2dl.mobe.miniprojetandroid">

    <uses-feature android:name="android.hardware.Camera" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logoapp"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".geolocalisation.GeolocalisationActivity" >
        </activity>
        <activity
            android:name=".emploidutemps.ScheduleActivity"
            android:label="@string/schedule" >
        </activity>
        <activity
            android:name=".occupationru.OccupationActivity"
            android:label="@string/occupRU" >
        </activity>
        <activity
            android:name=".qrcode.Qrcodescanner"
            android:label="QrCode" >
        </activity>
        <activity
            android:name=".photo.VisualizePhotoActivity"
            android:label="@string/anomalie" >
        </activity>
        <activity
            android:name=".photo.PhotosActivity"
            android:label="@string/anomalie" >
        </activity>
        <activity
            android:name=".configuration.ConfigurationActivity"
            android:label="@string/config">
        </activity>

    </application>

</manifest>